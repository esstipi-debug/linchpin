# CLAUDE.md — Kern

> Auto-loaded every session. High-signal map so an agent starts **understanding**
> the project instead of re-deriving it. When something here is stale, fix it in
> the same PR that changed the underlying fact.

## What Kern is

> Renamed from **Linchpin** on 2026-07-12 — narrative in
> [documentation/KERN_IDENTIDAD_Y_FILOSOFIA.md](documentation/KERN_IDENTIDAD_Y_FILOSOFIA.md).
> Infra identifiers (repo name, linchpin.fly.dev, LINCHPIN_* env vars, linchpin_*
> MCP tool names, odoo_addon/linchpin_dry_run) intentionally still say "linchpin"
> until the external checklist in HANDOFF.md is executed.

An **agentic supply-chain engine**: a plain-language brief becomes a finished,
QA-gated deliverable (Excel + report + chart), grounded in a knowledge graph of
25 SCM sources (24 books + an AI-in-SC source) and the codebase itself. A deterministic Python **engine** across
**41 agent-routable tools** (EOQ, safety stock, `(s,Q)`/`(R,S)`, simulation,
a network digital twin, forecasting, pricing, price intelligence,
discovery-assisted price watch, DDMRP, ABC-XYZ, sourcing, landed cost,
cost-to-serve, S&OP, facility location, DRP, transportation, FEFO, launch
readiness, vehicle routing…) sits under an
**orchestrator agent** that classifies → runs → validates → delivers. Runs
with or without an LLM.

Positioning: **the human sells & decides, Kern produces 10×.** Every
consequential result ends in one of four outcomes — and three need a human
(see *Never-unprotected* below).

## Project map

| Path | Role |
|---|---|
| `scm_agent/` | Orchestrator: `intent` → `registry`/`tools` → `prepare→run→qa→deliver`. Also `modes.py` (Inventory vs SCM personas), `knowledge.py` (L3 query surface), `guided_bridge.py`, `llm.py` |
| `src/` | Pure-function engine. One module = one question (`eoq.py`, `safety_stock.py`, `policies.py`, `forecasting.py`, `guided.py`, `writeback.py`, `escalation.py`, …) |
| `jobs/` | Per-tool data-prep playbooks (`<x>_job.py`, pandas-only `prepare()`) + deliverable builders |
| `webapp/` | FastAPI dashboard + `POST /api/jobs` + live agent console (`static/prototype/`) |
| `examples/` | Runnable CLIs (`run_agent.py`, `query_knowledge.py`, `run_*_job.py`, `run_price_intel.py`, `run_price_watch.py` (discovery-assisted price watch, end-to-end), `run_repricing.py` (multichannel repricing playbook, offline stand-in), parts 1–4) |
| `knowledge/scm-books/` | **Books graph** (L3 theory, 25 sources: 24 books/papers + an AI-in-SC source, committed) |
| `graphify-out/` | **Code graph** (gitignored, regenerated by the SessionStart hook) |
| `tests/` | 1100+ tests (engine vs. book numbers + agent + HTTP guards). `PYTHONPATH=.` |
| `documentation/operator/` | Operator Portfolio — the human's role, runbooks, methodologies |

## How the agent works

`brief → intent.classify → registry.get(tool) → prepare → run → QA → deliver → JobResult`

- **"QA fails ⇒ no deliverable"** is enforced in one place (the orchestrator).
- Statuses: `ok` · `needs_clarification` · `needs_data` · `qa_failed` · `error`.
- Adding a capability = one `register()` call + a `jobs/<x>_job.py`; no routing edits.

### Never-unprotected (`src/guided.py`)
Every consequential result is a `GuidedOutcome` with status:
`EXECUTED` (agent did it) · `OPTIONS` (ranked choices) · `HANDOFF` (prepared
human step) · `ESCALATED` (routed to a human with SLA). The last three need a
human — that contract is what `documentation/operator/` documents.

### Writeback safety (`src/writeback.py`)
Never mutate a system of record blindly: stage a dry-run `Changeset`, classify by
risk tier (`read`/`reversible`/`irreversible`), gate on a time-boxed `Approval`
(900 s TTL), apply idempotently, `rollback()`-able. Irreversible always needs
explicit approval. Real connectors implementing the store surface: Odoo
(`src/connectors/odoo.py`) and client Excel workbooks (`src/connectors/excel.py`
— drift check vs the live file, atomic temp+replace write, per-commit file
backup, cell-exact rollback; `.xlsm` macros preserved via `keep_vba`, never
executed).

### Discovery-assisted price watch (`jobs/price_watch.py`, read-only)
Auto-onboard a never-seen competitor site, homologate its product pages against
your catalog, then re-acquire and monitor the confirmed pairs on a recurring
cadence. Two hard invariants: it is **100% read-only observation** — no price is
ever written back to any competitor, marketplace, or your own catalog anywhere
in the pipeline (`src/writeback.py` is never imported); and **auto-onboarding is
robots.txt-only and always `limited`** (`tos_decision` is never `"allowed"`,
`max_tier_allowed` never above `L1` — robots.txt says nothing about a site's
ToS, so no automated basis to claim a human legal review). A tier raise beyond
the approved ceiling is surfaced as a pending-approval `GuidedOutcome` (R5),
never applied alone. End-to-end CLI: `examples/run_price_watch.py`
(`--url <competitor_url> --catalog <our_catalog.csv>`, or `--demo`); acceptance
proof in `tests/test_price_watch_e2e.py`.

### Client parameter profiles (`src/client_profile.py`)
Per-client durable cost/capacity parameters (holding rate, order cost, service
level, lead time, warehouse capacity), keyed by a slugified client name under
`clients/<slug>/profile.json`. The orchestrator merges the profile into the
request's `params` before `tool.prepare` AND `tool.run` (both see the same
merged dict) — profile values only fill gaps, an explicit `overrides`/`params`
value always wins, and profile `lead_time_days` is intake's default lead time
only where the CSV carries none. Pass `Orchestrator.run(..., strict_params=True)`
(CLI: `--strict-params`) to make a tool's `required_client_params` (see
`scm_agent/registry.py`) block with `needs_clarification` instead of silently
taking the engine's generic default (0.95 / 0.25 / $75) — the answer, once
supplied, is persisted via `client_profile.upsert_profile(...)` so it is asked
only once per client. Trust boundary: generic labels ("Client", "MCP client")
never resolve a profile; `Orchestrator(clients_root=None)` disables profiles
entirely and is what the webapp/MCP surface uses by default (the `client` field
there is a caller-typed label, not an authenticated identity — opt in locally
via `LINCHPIN_CLIENTS_ROOT`). Not a connector to the client's own system of
record (that's `src/writeback.py`).

## Understand the code fast (don't just grep — query the graphs)

The SessionStart hook keeps the **code graph** fresh. Use it before re-reading files:

```bash
# Structure (code graph): what connects to what, impact, paths
graphify query "how does the orchestrator route a brief to a tool" --budget 800
graphify explain "KnowledgeBase"
graphify affected "writeback.apply" --depth 2      # what breaks if I change X
graphify path "Orchestrator" "Deliverable"

# Domain + theory↔code bridge (L3 books graph + code graph)
python examples/query_knowledge.py --bridge "safety stock"   # chapter AND src/ fn
python examples/query_knowledge.py --search "intermittent demand"
python examples/query_knowledge.py --explain crostons_method
```

A `graphify` MCP server is also wired in `.mcp.json` (serves
`graphify-out/graph.json`) — its query tools appear when the binary is present.

Two complementary code-intel MCP servers are wired alongside it (adopted
2026-07-12 after an alternatives review — see `graphify-alternatives-verdict`
in project memory; graphify stays canonical for the books graph / citations):
- **codegraph** — continuous zero-LLM code index (`codegraph init` once per
  clone builds `.codegraph/`, gitignored; auto-syncs on file changes).
- **serena** — LSP-over-MCP (pyright) for symbol-exact find-references /
  rename; runs via `uvx`, writes `.serena/` (gitignored).
LightRAG was evaluated and deliberately NOT adopted (only as a future shadow
lane if books-graph retrieval ever bottlenecks).

### Cross-session memory (the graphify loop)
The code graph is rebuilt each session, but **what you learn persists** in
`knowledge/graph-memory/` (committed). Aggregated lessons:
[`documentation/GRAPH_LESSONS.md`](documentation/GRAPH_LESSONS.md) — **read it at
session start** for orientation. When a structural query proves `useful` (or hits
a `dead_end`, or you `corrected` a wrong turn), record it so the next session
doesn't re-derive it, then regenerate the lessons and commit both in your PR:

```bash
graphify save-result --memory-dir knowledge/graph-memory --type query --outcome useful \
  --question "..." --answer "..." --nodes "NodeA" "NodeB"
graphify reflect --memory-dir knowledge/graph-memory \
  --out documentation/GRAPH_LESSONS.md --graph graphify-out/graph.json
```

See [`knowledge/graph-memory/README.md`](knowledge/graph-memory/README.md).

## Working with Claude on this project

- **Decision points get alternatives, never open questions.** When a next step
  genuinely needs the user's decision, present 2-4 concrete options (e.g. via
  `AskUserQuestion` or a lettered list) instead of an open "what do you want
  me to do?" prompt.

## Dev conventions

- **Python 3.11+**. Tests: `pytest tests/ -q` with `PYTHONPATH=.` (the hook persists it).
- **Lint scope** (matches CI): `ruff check src tests examples`.
- The **SessionStart hook** (`.claude/hooks/session-start.sh`) auto-installs
  `requirements-dev.txt` (web only) and refreshes `graphify-out/`. It's idempotent.
- **Workflow:** feature branch → **draft PR** → CI green (`tests` on py3.11/3.12/3.13)
  → **squash-merge** to `main`. Never push straight to `main`.
- New agent tool recipe: `jobs/<x>_job.py` with a pandas-only `prepare()` (read its
  own CSV, **not** `intake.py`) → `run`/`verify` → a `Tool` in `scm_agent/tools.py`
  with distinctive multi-word `intent_keywords`.

## Gotchas (read before committing)

- **ASCII-only in console prints** (Windows cp1252 breaks on em dashes). Markdown
  files written utf-8 are fine.
- **Gitignored & regenerable:** `graphify-out/`, `data/`, `deliverables/`, `.env`.
  Don't commit them. `.claude/settings.json` + `.claude/hooks/` **are** versioned;
  `.claude/settings.local.json` is personal.
- **`clients/` is gitignored but NOT regenerable** — it holds real per-client cost/
  capacity parameters (`src/client_profile.py`), hand-answered by an operator, not
  derived from anything else in the repo. Back it up separately; losing it just
  means every client's numbers silently fall back to the generic defaults again.
- **Never read or surface PII** — some datasets (e.g. DataCo) carry customer PII;
  analysis is aggregate-only.
- **Don't paste secrets** (API keys) into chat or commits.
- **Prod boots on `.[web,mcp]` only** — the `prod-boot` CI job imports `webapp.app`
  with just the production extras installed (mirrors the Dockerfile). Never add a
  *module-level* import of an optional-extra dep (`bs4`, `rapidfuzz`, `price-parser`,
  `python-stdnum`, `extruct`, ...) to any module on the app's boot chain
  (`webapp.app` → `scm_agent` → `tools` → `jobs` → `src`): import it lazily inside the
  function that uses it, or the app crashes on boot even though the full suite/CI stays
  green (those deps are present transitively in dev). See
  `tests/test_pricing_intel_boot_safety.py` and the `prod-boot` job in
  `.github/workflows/tests.yml`.
- The **books graph** (L3) needs an LLM backend (Kimi/Gemini) to rebuild; the
  **code graph** is AST-only (no key) and the hook handles it.

## Where to learn more

`HANDOFF.md` (session-to-session context) · `README.md` (full overview) ·
`scm_agent/README.md` (agent reference) · `documentation/METHODOLOGY.md` (models) ·
`documentation/operator/` (the human operator's portfolio).
