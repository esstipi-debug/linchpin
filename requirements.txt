# Canonical dependency source is pyproject.toml. This file mirrors the runtime
# core so `pip install -r requirements.txt` works; for the full install prefer:
#   pip install -e ".[web]"     # engine + web UI
#   pip install -e ".[dev]"     # + tests / lint
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
openpyxl>=3.1
